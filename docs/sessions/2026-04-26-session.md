# Session 2026-04-26

## Ziel der Session
Ein strukturiertes Multi-Agenten-System für das Copy-Trade-Projekt einrichten, das als Grundlage für sichere und professionelle Zusammenarbeit dient.

## Umgesetzt
- `AGENTS.md` erstellt mit definierten Agenten, Verantwortungen und Arbeitsregeln
- `.vscode/copilot-instructions.md` angelegt mit verbindlichen Arbeits- und Sicherheitsregeln
- `docs/sessions/2026-04-26-session.md` angelegt als erste Session-Notiz

## Geändert
- Neue Datei: `AGENTS.md`
- Neue Datei: `.vscode/copilot-instructions.md`
- Neue Datei: `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Das Multi-Agenten-System wird projektweit als Referenz verwendet
- Sicherheitsrichtlinien werden strikt durchgesetzt
- Dokumentationsagenten dürfen keinen Code ändern

## Offene Punkte
- OFFEN: Detailliertes Setup für Agenten-Workflows in vorhandenen Projekt-Dateien
- OFFEN: Integration von Agenten-Prüfungen in CI/CD

## Risiken
- Wenn das System nur dokumentarisch bleibt, besteht das Risiko, dass Regeln nicht praktisch eingehalten werden
- Ohne automatische CI-Verknüpfung kann die Agentensteuerung leicht umgangen werden

## Nächste Schritte
- Agenten-Regeln aktiv bei nächsten Code-Änderungen anwenden
- Projektarchitektur und Sicherheitschecks auf Basis der Agentendefinition prüfen
- Offene Punkte konkretisieren und in Task-Dokumentation aufnehmen

---

# Session-Ergaenzung: Projektueberblick

## Ziel der Session
Projektstand, Architektur, Einstiegspunkte und aktuelle Verifikation des Copy-Trade-V1-Neustarts erfassen.

## Umgesetzt
- Projektstruktur geprueft: `apps/`, `workers/`, `packages/`, `infra/`, `docs/`, `.agents/`
- Zentrale Architekturquellen gelesen: `README.md`, `docs/restart/*`, `docs/adr/*`
- API-, Domain-, Adapter- und Copy-Engine-Skeletons geprueft
- Lokale Test- und Lint-Kommandos ueber `.venv` verifiziert

## Geaendert
- Diese Session-Ergaenzung in `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Keine Codeaenderungen vorgenommen
- Der aktuelle Stand wird als Foundation-/Neustart-Basis bewertet, nicht als produktive Trading-Plattform

## Offene Punkte
- OFFEN: `packages/exchange_adapters/tests` fehlt, obwohl README/Standard-Testkommando diesen Pfad referenziert
- OFFEN: Der globale `python`-Befehl zeigt auf einen nicht zugreifbaren Windows-Store-Alias; `.venv\Scripts\python.exe` funktioniert
- OFFEN: Docker ist laut vorhandener Environment-Dokumentation noch nicht verfuegbar
- OFFEN: Datenmodell/Migrationen sind vorbereitet, aber noch ohne erste Tabellen
- OFFEN: NATS JetStream Wrapper, echte Exchange-Adapter und Copy-Execution sind noch nicht implementiert

## Risiken
- Copy-Trading-spezifische Sicherheitslogik ist bisher nur dokumentiert und in Domain-Schemas vorbereitet
- `ready`-Endpoint prueft noch keine echten Abhaengigkeiten wie PostgreSQL, Redis oder NATS
- Exchange-API-Aussagen muessen vor Implementierung erneut gegen offizielle Quellen verifiziert werden

## Naechste Schritte
- Testkommando oder fehlenden `packages/exchange_adapters/tests`-Ordner konsistent machen
- Datenmodell und erste Alembic-Migration fuer Foundation-Tabellen umsetzen
- NATS JetStream Wrapper und Copy-Engine-Subscription-Skeleton implementieren
- Readiness-Checks fuer PostgreSQL, Redis und NATS ergaenzen

---

# Session-Ergaenzung: Wiederaufbau nach PC-Neuaufsetzung

## Ziel der Session
Lokale Entwicklungsumgebung nach PC-Neuaufsetzung erneut pruefen und die Foundation-Checks wieder stabilisieren.

## Umgesetzt
- `.venv` geprueft: Python 3.11.9 funktioniert
- Fehlendes `pip` in `.venv` mit `ensurepip` wiederhergestellt
- Fehlende Python-Abhaengigkeiten mit `pip install -r requirements.in` nachinstalliert
- `.vscode/settings.json` auf workspace-lokale `.venv` umgestellt
- Fehlenden Testpfad `packages/exchange_adapters/tests` mit Adapter-Basistests angelegt
- README- und Runbook-Kommandos auf repo-lokale `.venv` und aktuellen Workspace angepasst
- Environment-Dokumentation um Rebuild-Status nach PC-Neuaufsetzung ergaenzt
- Verifikation: `pip check` sauber, `pytest` 8 passed, Ruff sauber
- API-Smoke-Test mit Uvicorn erfolgreich fuer `/health`, `/ready` und `/version`
- Zusatz-Preflight: Docker, Node und npm sind aktuell nicht verfuegbar

## Geaendert
- `.vscode/settings.json`
- `README.md`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `packages/exchange_adapters/tests/test_base.py`

## Entscheidungen
- Projektchecks verwenden vorerst explizit `.venv\Scripts\python.exe`, weil globales `python` nicht verlaesslich ist
- Kein Git- oder Docker-Fix per Code, da beide Tools systemweit installiert beziehungsweise in PATH gebracht werden muessen

## Offene Punkte
- OFFEN: Git ist unter `D:\Git\cmd\git.exe` installiert, aber in der aktuellen Shell nicht im PATH
- OFFEN: Python 3.11 systemweit installieren oder Windows-App-Alias deaktivieren
- OFFEN: Docker Desktop installieren und `docker compose` pruefen
- OFFEN: Node.js LTS installieren, bevor Frontend-Arbeiten starten

## Risiken
- Ohne Git im PATH muessen Git-Kommandos vorerst ueber `D:\Git\cmd\git.exe` oder VS-Code-Workspace-Konfiguration laufen
- Ohne Docker kann die Infrastruktur mit PostgreSQL, Redis und NATS nicht lokal gestartet werden

## Naechste Schritte
- Git/Python/Docker auf dem Windows-System installieren oder PATH reparieren
- Danach Docker Compose Config und API-Smoke-Test aus dem Runbook ausfuehren

---

# Session-Ergaenzung: Systemvoraussetzungen nachgezogen

## Ziel der Session
Fehlende lokale Systemvoraussetzungen fuer Backend-, Frontend- und Docker-Entwicklung installieren und verifizieren.

## Umgesetzt
- Git-PATH fuer den Benutzer ergaenzt; `git --version` meldet `2.54.0.windows.1`
- Python 3.11.9 systemweit verfuegbar gemacht; `python` und `py` melden `Python 3.11.9`
- PowerShell ExecutionPolicy fuer den Benutzer auf `RemoteSigned` gesetzt, damit `npm.ps1` funktioniert
- Node.js LTS per Winget installiert; `node --version` meldet `v24.15.0`, `npm --version` meldet `11.12.1`
- Docker Desktop per Winget installiert; Version `4.70.0`
- Docker CLI verifiziert; `docker --version` meldet `Docker version 29.4.0`
- Docker Compose verifiziert; `docker compose version` meldet `v5.1.2`
- Projekt-Compose-Konfiguration erfolgreich validiert
- WSL und `VirtualMachinePlatform` als Windows-Features aktiviert

## Geaendert
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Docker-Installation wurde nach fehlerhaft angelegtem `C:\ProgramData\DockerDesktop` repariert, indem der leere, falsch gehoerende Ordner entfernt und der Installer erneut als Administrator gestartet wurde
- Kein automatischer Windows-Neustart ausgefuehrt

## Offene Punkte
- OFFEN: Windows neu starten, damit WSL/VirtualMachinePlatform wirksam werden
- OFFEN: Docker Desktop meldet `Virtualization support not detected`; Windows meldet `VirtualizationFirmwareEnabled: False`
- OFFEN: Falls ein Neustart nicht reicht, Intel Virtualization Technology im BIOS/UEFI des Gigabyte Z790 AORUS MASTER aktivieren
- OFFEN: Nach Neustart Docker Desktop starten und `docker info` pruefen
- OFFEN: Danach `docker compose -f infra\docker\docker-compose.yml --env-file infra\docker\.env.example up --build` testen

## Risiken
- Docker Engine ist vor dem Neustart noch nicht nutzbar; `docker info` erreicht den Server/Daemon noch nicht erfolgreich
- Neue Terminals oder VS Code-Fenster muessen nach PATH-Aenderungen ggf. neu gestartet werden
- BIOS/UEFI-Virtualisierung kann nicht aus dem Projekt heraus aktiviert werden und blockiert Docker/WSL2 bis zur manuellen Aktivierung

## Naechste Schritte
- Windows neu starten
- Falls Docker weiter blockiert: BIOS/UEFI oeffnen und Intel Virtualization Technology aktivieren
- Nach Neustart Docker Engine und lokalen Compose-Stack verifizieren

---

# Session-Ergaenzung: Docker Stack nach Neustart verifiziert

## Ziel der Session
Docker Desktop nach dem Windows-Neustart pruefen und den lokalen Compose-Stack vollstaendig starten.

## Umgesetzt
- Docker Engine nach Start von Docker Desktop erfolgreich verifiziert
- `docker info` meldet `29.4.0 Docker Desktop 32`
- Lokalen Compose-Stack mit Build gestartet
- NATS-Healthcheck-Problem behoben: `nats:2.10` auf `nats:2.10-alpine` umgestellt
- Copy-Engine-Skeleton als long-running Worker angepasst
- API-, NATS-, PostgreSQL-, Redis- und Copy-Engine-Container laufen
- API-Smoke-Test im Docker-Stack erfolgreich fuer `/health`, `/ready` und `/version`
- NATS-Monitoring-Healthcheck auf `http://127.0.0.1:8222/healthz` erfolgreich

## Geaendert
- `infra/docker/docker-compose.yml`
- `workers/copy_engine/src/copy_trade_copy_engine/main.py`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- NATS nutzt lokal `nats:2.10-alpine`, weil das vorherige Image keine Shell fuer den vorhandenen Healthcheck bereitstellt
- Copy Engine bleibt im Skeleton-Modus aktiv laufend, bis die echte NATS-Subscription implementiert wird

## Offene Punkte
- OFFEN: `/ready` prueft noch keine echten Verbindungen zu PostgreSQL, Redis und NATS
- OFFEN: Copy Engine subscribed noch nicht auf NATS und verarbeitet noch keine Events

## Risiken
- Docker-Stack ist lokal lauffaehig, aber die fachliche Execution-Pipeline ist weiterhin nur Skeleton

## Naechste Schritte
- Readiness-Checks fuer PostgreSQL, Redis und NATS implementieren
- NATS Event Bus Wrapper und Copy-Engine-Subscription-Skeleton umsetzen

---

# Session-Ergaenzung: API Readiness Checks

## Ziel der Session
Den `/ready`-Endpoint von statischer Antwort auf echte Dependency-Pruefungen fuer PostgreSQL, Redis und NATS umstellen.

## Umgesetzt
- Neues Readiness-Modul fuer PostgreSQL-, Redis- und NATS-Pruefungen erstellt
- `/ready` gibt bei Erfolg `status=ready` und Dependency-Status fuer `postgres`, `redis`, `nats` zurueck
- `/ready` gibt bei fehlender Dependency HTTP 503 mit `status=not_ready` zurueck
- Exception-Details werden nicht in der API-Antwort ausgegeben
- API-Tests fuer ready und not-ready ergaenzt
- Dev-Abhaengigkeiten in `pyproject.toml` an Runtime-Abhaengigkeiten angeglichen

## Geaendert
- `apps/api/src/copy_trade_api/main.py`
- `apps/api/src/copy_trade_api/readiness.py`
- `apps/api/tests/test_readiness.py`
- `pyproject.toml`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Readiness-Checks nutzen kurze Timeouts und geben nur `ok` oder `unavailable` aus, damit keine Connection-Details oder Secrets geleakt werden
- Unit-Tests injizieren einen Fake-Readiness-Checker, damit sie ohne Docker deterministisch bleiben

## Offene Punkte
- OFFEN: Copy Engine subscribed noch nicht auf NATS und verarbeitet noch keine Events
- OFFEN: NATS Event Bus Wrapper fehlt noch

## Risiken
- `/ready` oeffnet aktuell pro Anfrage frische Verbindungen zu PostgreSQL, Redis und NATS; fuer lokale Foundation ok, spaeter ggf. mit langlebigen Clients/Pool pruefen

## Naechste Schritte
- NATS Event Bus Wrapper implementieren
- Copy-Engine-Subscription-Skeleton fuer `exchange.trade_event.normalized` umsetzen

---

# Session-Ergaenzung: NATS Event Bus und Copy-Engine Subscription

## Ziel der Session
NATS JetStream als internen Event-Bus nutzbar machen und die Copy Engine an normalisierte Trade Events anbinden.

## Umgesetzt
- Neues Package `copy_trade_shared_events` unter `packages/shared_events/src`
- NATS-Subject-Konstanten und Stream `COPY_TRADE_EVENTS` angelegt
- JetStream-Wrapper fuer JSON Publish/Subscribe implementiert
- Publish-Header `Nats-Msg-Id` wird aus `idempotency_key` erzeugt
- Copy Engine liest `COPY_TRADE_NATS_URL` aus eigener Worker-Konfiguration
- Copy Engine subscribed durable auf `exchange.trade_event.normalized`
- Copy Engine validiert empfangene Payloads als `NormalizedOrderEvent`
- Copy Engine loggt empfangene Events ohne Raw-Payload und ohne Account-ID
- Dockerfiles und Python-Konfiguration um `packages/shared_events/src` erweitert
- Unit-Tests fuer Event-Subjects, JSON-Encoding, Idempotency-Header, async Callback und Copy-Engine-Subscription ergaenzt
- Unit-Tests fuer Ack/Nak-Verhalten des Event-Bus-Message-Handlers ergaenzt
- Docker-Smoke-Test: normalisiertes Test-Event publiziert und in Copy-Engine-Logs empfangen

## Geaendert
- `packages/shared_events/src/copy_trade_shared_events/__init__.py`
- `packages/shared_events/src/copy_trade_shared_events/bus.py`
- `packages/shared_events/src/copy_trade_shared_events/subjects.py`
- `packages/shared_events/tests/test_bus.py`
- `workers/copy_engine/src/copy_trade_copy_engine/config.py`
- `workers/copy_engine/src/copy_trade_copy_engine/main.py`
- `workers/copy_engine/tests/test_main.py`
- `infra/docker/api.Dockerfile`
- `infra/docker/worker.Dockerfile`
- `pyproject.toml`
- `README.md`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `packages/shared_events/README.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- JetStream Stream-Subjects werden explizit statt per breitem Wildcard definiert
- Duplicate-Detection-Vorbereitung erfolgt ueber `Nats-Msg-Id` aus `idempotency_key`
- Subscription-Callback wird als echte async Funktion gebaut, weil nats-py synchrone Lambdas ablehnt
- Copy Engine fuehrt noch keine Orders aus und erzeugt noch keine Copy-Execution-Requests

## Offene Punkte
- OFFEN: Copy Engine erzeugt noch keine Copy-Execution-Requests aus empfangenen Events
- OFFEN: Es gibt noch keinen Idempotency Store in PostgreSQL
- OFFEN: Retry-/DLQ-Konzept fuer dauerhaft fehlerhafte Events fehlt noch

## Risiken
- Durable Consumer verarbeitet aktuell alle Events ab Stream-Anfang; vor Produktivbetrieb muss Consumer-/Backfill-Verhalten explizit entschieden werden
- Ohne Idempotency Store darf aus empfangenen Events noch keine echte Order-Ausfuehrung entstehen

## Naechste Schritte
- Idempotency Store und erste Datenmodell-Migration vorbereiten
- Copy-Engine-Verarbeitung von NormalizedOrderEvent zu Dry-Run CopyExecutionRequest erweitern
- Retry-/DLQ-Konzept fuer NATS JetStream dokumentieren

---

# Session-Ergaenzung: Dry-Run CopyExecutionRequest Processor

## Ziel der Session
Empfangene `NormalizedOrderEvent`s in der Copy Engine fachlich bis zu sicheren Dry-Run-`CopyExecutionRequest`s verarbeiten, ohne echte Orders auszufuehren.

## Umgesetzt
- Copy-Engine-Processor fuer `NormalizedOrderEvent` angelegt
- Relationship-Abstraktion fuer aktive Copy-Beziehungen eingefuehrt
- Prozesslokalen In-Memory-Idempotency-Store eingefuehrt
- Processor erzeugt `CopyExecutionRequest`s ueber die vorhandene Domain-Funktion `build_dry_run_execution_request`
- Inaktive Copy-Beziehungen werden uebersprungen
- Events vor `effective_from` einer Copy-Beziehung werden uebersprungen
- Duplicate-Events mit gleicher Request-Idempotency werden uebersprungen
- Copy Engine publiziert erzeugte Requests auf `copy.execution.requested`
- Copy Engine loggt nur Event-ID, Trace-ID und Verarbeitungszaehler
- Unit-Tests fuer Processor, Idempotency, Event-Bus-Publish und Subscription-Fluss ergaenzt
- Docker-Smoke-Test: `exchange.trade_event.normalized` publiziert und von der Copy Engine verarbeitet

## Geaendert
- `packages/shared_events/src/copy_trade_shared_events/__init__.py`
- `workers/copy_engine/src/copy_trade_copy_engine/idempotency.py`
- `workers/copy_engine/src/copy_trade_copy_engine/relationships.py`
- `workers/copy_engine/src/copy_trade_copy_engine/processor.py`
- `workers/copy_engine/src/copy_trade_copy_engine/main.py`
- `workers/copy_engine/tests/test_main.py`
- `workers/copy_engine/tests/test_processor.py`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Die Copy Engine bleibt weiterhin im Dry-Run-Modus; echte Order-Ausfuehrung ist nicht angeschlossen
- Zu diesem Zeitpunkt war der Default-Relationship-Provider noch leer; spaeter in der Session wurde er durch einen DB-Provider ersetzt
- Zu diesem Zeitpunkt war Idempotency nur pro Prozesslauf stabil; spaeter in der Session wurde ein PostgreSQL-Store ergaenzt
- Request-Publishing nutzt den bestehenden NATS Event-Bus und den neuen Subject `copy.execution.requested`

## Offene Punkte
- OFFEN: PostgreSQL-Datenmodell fuer Copy-Beziehungen und Idempotency fehlt noch
- OFFEN: Alembic-Migration fuer Foundation-Tabellen fehlt noch
- OFFEN: Copy Engine liest aktive Copy-Beziehungen noch nicht aus der Datenbank
- OFFEN: Retry-/DLQ-Konzept fuer dauerhaft fehlerhafte Events fehlt noch
- OFFEN: Es gibt weiterhin keine echte Exchange-Order-Ausfuehrung

## Risiken
- In-Memory-Idempotency verhindert Duplikate nur innerhalb eines laufenden Worker-Prozesses; fuer den produktiven Startpfad wurde spaeter ein PostgreSQL-Store ergaenzt
- Ohne DB-Provider erzeugt der Docker-Smoke-Test keine `CopyExecutionRequest`s aus realen Beziehungen
- Vor echter Order-Ausfuehrung muessen Risiko-, Slippage-, Balance- und Permission-Checks ergaenzt werden

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 33 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events` erfolgreich
- `.venv\Scripts\python.exe -m pip check` erfolgreich
- Docker-Stack erfolgreich neu gebaut und gestartet
- `/ready` meldet `postgres=ok`, `redis=ok`, `nats=ok`
- Copy-Engine-Log zum Smoke-Event: `requests=0`, `duplicates=0`, `inactive=0`, `before_follow_start=0`

## Naechste Schritte
- PostgreSQL-Tabellen und Alembic-Migration fuer Copy-Beziehungen, Event-Idempotency und Execution-Requests anlegen
- DB-gestuetzten Relationship-Provider fuer die Copy Engine implementieren (spaeter in dieser Session umgesetzt)
- Dauerhafte Idempotency-Reservierung mit Transaktionsschutz implementieren

---

# Session-Ergaenzung: PostgreSQL Foundation fuer Copy Engine

## Ziel der Session
Die Copy Engine von prozesslokaler In-Memory-Grundlage auf eine dauerhafte PostgreSQL-Basis fuer Copy-Beziehungen, Idempotency und Execution-Requests vorbereiten.

## Umgesetzt
- Erste Alembic-Migration `20260426_0001` angelegt
- Tabelle `copy_relationships` fuer aktive Copy-Beziehungen angelegt
- Tabelle `copy_execution_idempotency` fuer dauerhafte Idempotency-Reservierung angelegt
- Tabelle `copy_execution_requests` fuer spaetere Request-Persistenz vorbereitet
- PostgreSQL-Relationship-Provider fuer die Copy Engine implementiert
- PostgreSQL-Idempotency-Store mit `INSERT ... ON CONFLICT DO NOTHING` implementiert
- PostgreSQL-Recorder fuer `CopyExecutionRequest`s implementiert
- Copy Engine startet standardmaessig mit DB-Provider und DB-Idempotency-Store
- Copy Engine persistiert erzeugte Dry-Run-Requests vor dem Publish auf `copy.execution.requested`
- Unit-Tests fuer URL-Normalisierung, DB-Relationship-Provider und DB-Idempotency ergaenzt
- Unit-Test fuer DB-Persistenz von `CopyExecutionRequest`s ergaenzt
- Runbook um Alembic-Kommandos ergaenzt

## Geaendert
- `README.md`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `infra/migrations/versions/20260426_0001_copy_execution_foundation.py`
- `workers/copy_engine/src/copy_trade_copy_engine/config.py`
- `workers/copy_engine/src/copy_trade_copy_engine/database.py`
- `workers/copy_engine/src/copy_trade_copy_engine/execution_requests.py`
- `workers/copy_engine/src/copy_trade_copy_engine/idempotency.py`
- `workers/copy_engine/src/copy_trade_copy_engine/main.py`
- `workers/copy_engine/src/copy_trade_copy_engine/relationships.py`
- `workers/copy_engine/tests/test_main.py`
- `workers/copy_engine/tests/test_postgres_persistence.py`

## Entscheidungen
- Idempotency wird transaktionssicher ueber einen eindeutigen PostgreSQL-Key reserviert
- Copy-Relationships koennen optional auf ein Source-Symbol eingeschraenkt werden
- Request-Persistenz erfolgt vor dem Event-Publish, damit erzeugte Dry-Run-Requests nachvollziehbar bleiben
- Lokale Smoke-Daten werden nach Verifikation wieder entfernt

## Offene Punkte
- OFFEN: API/UI fuer Copy-Relationship-Verwaltung fehlt noch
- OFFEN: Request-Status-Lifecycle und `CopyExecutionResult`-Persistenz wurden spaeter in dieser Session umgesetzt
- OFFEN: Risk Settings, Subscription-Gates und Exchange-Verbindungsvalidierung fehlen noch vor echter Order-Ausfuehrung
- OFFEN: Retry-/DLQ-Konzept fuer dauerhaft fehlerhafte Events fehlt noch

## Risiken
- Zu diesem Zeitpunkt blieben Requests im Status `REQUESTED`; spaeter in dieser Session wurde der Status-Lifecycle bis Result-Events ergaenzt
- Ohne API/Auth-Schicht duerfen Copy-Relationships noch nicht von extern verwaltet werden
- Vor echter Ausfuehrung fehlen weiterhin Risiko-, Balance-, Slippage-, Permission- und Subscription-Pruefungen

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 33 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events infra\migrations` erfolgreich
- `.venv\Scripts\python.exe -m alembic upgrade head` erfolgreich
- `.venv\Scripts\python.exe -m alembic current` meldet spaeter in dieser Session `20260426_0004 (head)`
- Docker-Stack erfolgreich neu gebaut und gestartet
- `/ready` meldet `postgres=ok`, `redis=ok`, `nats=ok`
- DB-Smoke-Test mit Test-Relationship erzeugte im Copy-Engine-Log `requests=1`
- Smoke-Verifikation fand je einen Datensatz in `copy_execution_requests` und `copy_execution_idempotency`
- Danach wurden Smoke-Datensaetze aus `copy_relationships`, `copy_execution_requests` und `copy_execution_idempotency` entfernt

## Naechste Schritte
- API-Endpunkte fuer Copy-Relationships mit Auth-/Validation-Gates vorbereiten
- Request-Status-Lifecycle und `CopyExecutionResult`-Persistenz implementieren (spaeter in dieser Session umgesetzt)
- Retry-/DLQ-Entscheidung fuer NATS JetStream dokumentieren

---

# Session-Ergaenzung: Execution Result Status Lifecycle

## Ziel der Session
Dry-Run-Execution-Requests nach dem Publish statusseitig nachfuehren und spaetere Execution-Result-Events dauerhaft speichern.

## Umgesetzt
- Zweite Alembic-Migration `20260426_0002` angelegt
- Tabelle `copy_execution_results` fuer Execution-Result-Events angelegt
- Copy Engine subscribed auf `copy.execution.accepted`, `copy.execution.rejected`, `copy.execution.filled` und `copy.execution.failed`
- Copy Engine persistiert `CopyExecutionResult`s in PostgreSQL
- Copy Engine setzt Request-Status nach erfolgreichem Request-Publish auf `PUBLISHED`
- Copy Engine setzt Request-Status nach Result-Event auf `ACCEPTED`, `REJECTED`, `FILLED` oder `FAILED`
- Idempotency-Reservierung wird wieder freigegeben, wenn das Publish des Request-Events fehlschlaegt
- Unit-Tests fuer Result-Handler, Result-Persistenz, Status-Update und Publish-Failure-Retry-Verhalten ergaenzt

## Geaendert
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `infra/migrations/versions/20260426_0002_copy_execution_results.py`
- `packages/shared_events/README.md`
- `packages/shared_events/src/copy_trade_shared_events/__init__.py`
- `packages/shared_events/src/copy_trade_shared_events/subjects.py`
- `workers/copy_engine/src/copy_trade_copy_engine/execution_requests.py`
- `workers/copy_engine/src/copy_trade_copy_engine/execution_results.py`
- `workers/copy_engine/src/copy_trade_copy_engine/idempotency.py`
- `workers/copy_engine/src/copy_trade_copy_engine/main.py`
- `workers/copy_engine/src/copy_trade_copy_engine/processor.py`
- `workers/copy_engine/tests/test_main.py`
- `workers/copy_engine/tests/test_postgres_persistence.py`

## Entscheidungen
- Result-Subjects bekommen eigene durable Consumer, damit Statusereignisse einzeln nachvollziehbar bleiben
- Result-Persistenz aktualisiert den Request-Status direkt nach dem Insert
- Request-Publish-Failure gibt Idempotency frei, damit NATS-Retry den Request erneut publizieren kann
- Es bleibt weiterhin bei Dry-Run; Result-Smoke nutzt ein simuliertes Result-Event

## Offene Punkte
- OFFEN: Retry-/DLQ-Policy wurde spaeter in dieser Session umgesetzt; DLQ-Reprocessing und Alerting fehlen noch
- OFFEN: Echte Exchange-Execution und echte Result-Produktion fehlen noch
- OFFEN: API-Verwaltung fuer Copy-Relationships wurde spaeter in dieser Session umgesetzt; UI fehlt noch
- OFFEN: Risk Settings, Subscription-Gates und Exchange-Verbindungsvalidierung fehlen noch

## Risiken
- Ohne DLQ kann ein dauerhaft fehlerhaftes Event weiter retryen und operative Aufmerksamkeit binden
- Status-Updates sind vorbereitet, aber noch nicht durch echte Exchange-Reconciliation abgesichert
- Vor echter Ausfuehrung fehlen weiterhin Risiko-, Balance-, Slippage-, Permission- und Subscription-Pruefungen

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 33 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events infra\migrations` erfolgreich
- `.venv\Scripts\python.exe -m alembic upgrade head` erfolgreich
- `.venv\Scripts\python.exe -m alembic current` meldet spaeter in dieser Session `20260426_0004 (head)`
- Docker-Stack erfolgreich neu gebaut und gestartet
- `/ready` meldet `postgres=ok`, `redis=ok`, `nats=ok`
- End-to-End-Smoke: Request wurde `PUBLISHED`, Result wurde gespeichert, Request wurde `FILLED`
- Smoke-Datensaetze wurden aus `copy_relationships`, `copy_execution_idempotency`, `copy_execution_requests` und `copy_execution_results` entfernt

## Naechste Schritte
- Retry-/DLQ-Policy fuer NATS JetStream dokumentieren und implementieren (spaeter in dieser Session umgesetzt)
- API-Endpunkte fuer Copy-Relationships mit Auth-/Validation-Gates vorbereiten
- Exchange-Execution-Adapter weiterhin nur nach erneuter offizieller API-Verifikation anschliessen

---

# Session-Ergaenzung: NATS Retry und Dead Letter Queue

## Ziel der Session
Dauerhaft fehlerhafte NATS-Events begrenzen, operational sichtbar machen und verhindern, dass Copy-Engine-Consumer endlos blockiert werden.

## Umgesetzt
- ADR `ADR-004-event-retry-dlq-policy.md` angelegt
- Dead-Letter-Subject `system.dead_letter.created` angelegt
- `system.dead_letter.created` in den Stream `COPY_TRADE_EVENTS` aufgenommen
- Event-Bus aktualisiert bestehende Streams, wenn neue Subjects fehlen
- Event-Bus konfiguriert Durable Consumer mit `ack_policy=explicit`, `ack_wait=30.0`, `max_deliver=3`
- Event-Bus aktualisiert bestehende Durable Consumer auf die erwartete Retry-Konfiguration
- Handler-Fehler werden bis zur finalen Zustellung per `nak` erneut zugestellt
- Nach finalem Fehler wird ein Dead-Letter-Event publiziert und das Originalevent per `ack` abgeschlossen
- Dead-Letter-Events enthalten `failed_subject`, `delivery_attempt`, `max_delivery_attempts`, `error_type` und Payload
- Erwartete Zwischen-Retries erzeugen keine Callback-Stacktraces mehr
- Unit-Tests fuer Retry-Konfiguration, Zwischen-Retry und Dead-Letter-Erzeugung ergaenzt

## Geaendert
- `docs/adr/ADR-004-event-retry-dlq-policy.md`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `packages/shared_events/README.md`
- `packages/shared_events/src/copy_trade_shared_events/__init__.py`
- `packages/shared_events/src/copy_trade_shared_events/bus.py`
- `packages/shared_events/src/copy_trade_shared_events/subjects.py`
- `packages/shared_events/tests/test_bus.py`

## Entscheidungen
- `max_deliver=3` fuer normale Copy-Engine-Durable-Consumer
- `ack_wait=30.0` Sekunden als konservativer Default fuer lokale Foundation und spaetere laengere Handler
- Dead-Letter-Subject ist explizit und nicht per Wildcard im Stream definiert
- Exception-Typ wird gespeichert, freie Exception-Details werden nicht als Log ausgegeben

## Offene Punkte
- OFFEN: DLQ-Reprocessing-Workflow fehlt noch
- OFFEN: Monitoring/Alerting fuer DLQ-Events fehlt noch
- OFFEN: DLQ-Aufbewahrung und Purge-Policy fehlen noch

## Risiken
- DLQ-Events enthalten den urspruenglichen internen Event-Payload; keine Secrets in Trade-Events aufnehmen
- Ohne Reprocessing-Tool muessen DLQ-Events vorerst manuell untersucht werden
- Ohne Alerting koennen DLQ-Events unbemerkt bleiben

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 48 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events infra\migrations` erfolgreich
- Docker-Stack erfolgreich neu gebaut und gestartet
- `/ready` meldet `postgres=ok`, `redis=ok`, `nats=ok`
- NATS-Stream enthaelt `system.dead_letter.created`
- Copy-Engine-Durable-Consumer stehen auf `max_deliver=3` und `ack_wait=30.0`
- Isolierter DLQ-Smoke erzeugte nach finalem Fehler ein Dead-Letter-Event
- Normaler Request/Result-Smoke bleibt erfolgreich: Request `PUBLISHED`, Result gespeichert, Request `FILLED`
- Smoke-Datensaetze in PostgreSQL wurden entfernt

## Naechste Schritte
- API-Endpunkte fuer Copy-Relationships mit Auth-/Validation-Gates vorbereiten
- DLQ-Reprocessing und Alerting vorbereiten
- Danach UI auf stabilen API-Contracts aufbauen

---

# Session-Ergaenzung: Admin Copy-Relationship API

## Ziel der Session
Einen sicheren Foundation-API-Contract fuer Copy-Relationships bereitstellen, damit spaeter UI und Worker auf derselben Datenbasis arbeiten koennen.

## Umgesetzt
- Admin-Token-Gate ueber Header `X-Copy-Trade-Admin-Token` angelegt
- `COPY_TRADE_ADMIN_API_TOKEN` in lokaler Docker-Example-Umgebung ergaenzt
- `POST /admin/copy-relationships` zum Anlegen von Copy-Beziehungen implementiert
- `GET /admin/copy-relationships` mit Filtern fuer `active` und `source_account_id` implementiert
- `PATCH /admin/copy-relationships/{relationship_id}` fuer Deaktivierung und Slippage-Anpassung implementiert
- Bewusst kein `DELETE`, damit Beziehungen nicht still entfernt werden
- API-Validierung fuer Exchanges, Slippage-Limit und timezone-aware `effective_from` ergaenzt
- Doppelte aktive Copy-Beziehungen per Migration verhindert
- API-Tests fuer Auth-Gate, nicht konfigurierte Admin-API, Create, List, Patch, 404, 409 und Validation ergaenzt

## Geaendert
- `apps/api/src/copy_trade_api/auth.py`
- `apps/api/src/copy_trade_api/config.py`
- `apps/api/src/copy_trade_api/copy_relationships.py`
- `apps/api/src/copy_trade_api/database.py`
- `apps/api/src/copy_trade_api/main.py`
- `apps/api/tests/test_copy_relationships.py`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `infra/docker/.env.example`
- `infra/migrations/versions/20260426_0003_unique_active_copy_relationship.py`

## Entscheidungen
- Die Relationship-Verwaltung bleibt vorerst unter `/admin/...`, weil echtes User-/Rollenmodell noch fehlt
- Mutierende und lesende Relationship-Endpunkte sind nicht offen, sondern Admin-Token-geschuetzt
- Keine Loesch-Endpunkte; Deaktivierung ist der sichere Foundation-Pfad
- Doppelte aktive Beziehungen werden in PostgreSQL verhindert, weil sie spaeter doppelte Copy-Requests erzeugen koennten

## Offene Punkte
- OFFEN: Echtes Auth-/User-/Rollenmodell fehlt noch
- OFFEN: UI fuer Copy-Relationship-Verwaltung fehlt noch
- OFFEN: Audit-Log fuer Relationship-Aenderungen wurde spaeter in dieser Session umgesetzt
- OFFEN: Subscription- und Exchange-Connection-Gates fehlen noch vor echter Ausfuehrung

## Risiken
- Der lokale Admin-Token in `.env.example` ist nur ein Platzhalter und darf nicht produktiv verwendet werden
- Ohne User-/Rollenmodell ist die API nur ein Admin-Foundation-Contract, kein finales Produkt-Auth-System
- Vor der spaeteren Audit-Umsetzung waren Relationship-Aenderungen noch nicht ausreichend nachvollziehbar fuer Produktivbetrieb

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 48 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events infra\migrations` erfolgreich
- `.venv\Scripts\python.exe -m alembic upgrade head` erfolgreich
- Docker-Stack erfolgreich neu gebaut und gestartet
- API-Smoke im Docker-Stack: Create/List/Patch fuer Copy-Relationship erfolgreich
- Smoke-Datensatz wurde aus `copy_relationships` entfernt

## Naechste Schritte
- Audit-Log fuer Relationship-Aenderungen vorbereiten (spaeter in dieser Session umgesetzt)
- Danach User-/Rollenmodell oder UI-Contract fuer Admin-Verwaltung entscheiden

---

# Session-Ergaenzung: Audit Logs fuer Admin Relationship Changes

## Ziel der Session
Admin-Aenderungen an Copy-Relationships transaktional und ohne Secret-Leakage nachvollziehbar machen.

## Umgesetzt
- Migration `20260426_0004` fuer `audit_logs` angelegt
- Audit-Log-Modul mit PostgreSQL-Repository und `GET /admin/audit-logs` implementiert
- Relationship-Create schreibt ein Audit-Event `copy_relationship.created`
- Relationship-Patch schreibt ein Audit-Event `copy_relationship.updated`
- Relationship-Write und Audit-Insert laufen in derselben PostgreSQL-Transaktion
- Patch-Audit enthaelt explizite `before_state`- und `after_state`-Snapshots
- Audit-Snapshots nutzen Allowlist-Felder aus der Relationship-Response
- Admin-Token und Request-Header werden nicht im Audit gespeichert
- Tests fuer Audit-Listen-Endpunkt, Create-Audit und Patch-Before/After-Audit ergaenzt
- Sub-Agent-Review fuer Audit-Risiken genutzt; Empfehlungen zu Transaktionalitaet und Secret-Vermeidung umgesetzt

## Geaendert
- `apps/api/src/copy_trade_api/audit.py`
- `apps/api/src/copy_trade_api/copy_relationships.py`
- `apps/api/src/copy_trade_api/main.py`
- `apps/api/tests/test_copy_relationships.py`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`
- `infra/migrations/versions/20260426_0004_admin_audit_logs.py`

## Entscheidungen
- Audit ist append-only und generisch genug fuer spaetere Admin-Aktionen
- `actor_type=admin_api`, `actor_id=NULL`, bis echtes User-/Rollenmodell existiert
- Keine Header, Tokens oder freie Exception-Details im Audit
- Erfolgreiche Create/Patch-Aenderungen werden auditiert; 401/422/404/409 werden vorerst nicht als Audit-Events geschrieben

## Offene Punkte
- OFFEN: Echtes User-/Rollenmodell muss spaeter konkrete Actor-IDs liefern
- OFFEN: Audit-Logging fuer fehlgeschlagene autorisierte Aktionen ist noch nicht entschieden
- OFFEN: Audit-Log-Retention und Export fehlen noch

## Risiken
- Audit-Logs enthalten interne Relationship-Daten und muessen spaeter rollenbasiert geschuetzt bleiben
- Ohne echtes Rollenmodell ist `admin_api` nur ein Foundation-Actor

## Verifikation
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 48 passed
- `.venv\Scripts\ruff.exe check apps\api workers\copy_engine packages\domain packages\exchange_adapters packages\shared_events infra\migrations` erfolgreich
- `.venv\Scripts\python.exe -m alembic upgrade head` erfolgreich
- Docker-Stack erfolgreich neu gebaut und gestartet
- API-Smoke im Docker-Stack: Create/Patch erzeugten zwei Audit-Events
- Audit-Response enthielt den lokalen Admin-Token nicht
- Smoke-Datensaetze wurden aus `audit_logs` und `copy_relationships` entfernt

## Naechste Schritte
- Echtes Auth-/User-/Rollenmodell vorbereiten
- Danach UI-Contract fuer Admin-Verwaltung oder DLQ-Reprocessing priorisieren

---

# Session-Ergaenzung: Sub-Agent-Freigabe

## Ziel der Session
Projektweite Freigabe fuer sinnvollen Sub-Agent-Einsatz dokumentieren.

## Umgesetzt
- `AGENTS.md` um Abschnitt `Sub-Agent-Freigabe` ergaenzt
- Codex darf separate Sub-Agenten fuer klar abgegrenzte Review- und Pruefaufgaben einsetzen
- Vor jedem Einsatz muss geprueft werden, ob ein Sub-Agent tatsaechlich Mehrwert bringt
- Hauptinstanz bleibt verantwortlich fuer Integration, Verifikation, Sicherheit und finale Zusammenfassung

## Geaendert
- `AGENTS.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- Sub-Agenten sind projektweit erlaubt, aber nicht automatisch Pflicht
- Einsatz bleibt sparsam, zielgerichtet und auf konkrete Aufgaben begrenzt

## Offene Punkte
- OFFEN: `.agent.md` ist im Workspace aktuell nicht vorhanden; die Regel wurde deshalb in `AGENTS.md` gesetzt

## Risiken
- Zu viele parallele Agenten koennen Review-Ergebnisse verwaessern; deshalb ist vorab eine Sinnpruefung Pflicht

---

# Session-Ergaenzung: Identity Foundation und DB Admin Credentials

## Ziel der Session
Den statischen Admin-Token-Fundamentpfad um eine DB-gestuetzte Identity-Basis mit Users, Rollen und gehashten API-Credentials erweitern.

## Umgesetzt
- Migration `20260426_0005` fuer `users`, `roles`, `user_roles` und `api_credentials` angelegt
- Rollen `admin`, `trader` und `follower` als stabile Foundation-Rollen geseedet
- Audit-Actor-Constraint um `user` erweitert
- Migration `20260426_0006` fuer UUID-Defaults und SHA-256-Hash-Constraint auf `api_credentials.token_hash` angelegt
- Neues Identity-Modul mit `AuthenticatedPrincipal`, Admin-Rollenpruefung und SHA-256 Token-Hashing erstellt
- DB-Admin-Auth prueft `api_credentials.active`, `users.status='active'` und Rolle `admin`
- `last_used_at` wird bei erfolgreicher DB-Admin-Authentifizierung aktualisiert
- Lokaler Env-Admin-Token bleibt nur fuer `local`/`test` als Bootstrap-Fallback erlaubt
- Bei DB-Auth schreiben Copy-Relationship-Audits jetzt `actor_type=user` und `actor_id=<user_id>`
- Tests fuer DB-Admin-Auth, Short-Token-Reject, lokalen Fallback, Prod-Bypass-Schutz und Audit-Attribution ergaenzt
- Security/QA-Sub-Agent fuer den Identity/Auth-Block eingesetzt; Findings zu Fallback, Audit-Attribution und Token-Hardening umgesetzt

## Geaendert
- `apps/api/src/copy_trade_api/auth.py`
- `apps/api/src/copy_trade_api/copy_relationships.py`
- `apps/api/src/copy_trade_api/identity.py`
- `apps/api/src/copy_trade_api/main.py`
- `apps/api/tests/test_copy_relationships.py`
- `apps/api/tests/test_identity.py`
- `infra/migrations/versions/20260426_0005_identity_foundation.py`
- `infra/migrations/versions/20260426_0006_identity_hardening.py`
- `docs/restart/03_foundation_runbook.md`
- `docs/restart/04_environment_verification.md`
- `docs/sessions/2026-04-26-session.md`

## Entscheidungen
- DB-Identity ist der normale Admin-Auth-Pfad; der Env-Token ist nur lokaler Bootstrap-Fallback
- Produktivumgebungen duerfen den Env-Token nicht als Bypass zur DB-Identity verwenden
- DB-Admin-Tokens muessen mindestens 32 Zeichen lang sein
- API-Credentials speichern keinen Klartext-Token
- User-spezifische Admin-Aktionen muessen im Audit als `user` mit konkreter Actor-ID sichtbar sein

## Offene Punkte
- OFFEN: Login-, Session-, JWT- und Passwortstrategie sind noch nicht entschieden
- OFFEN: Credential-Erzeugung, Rotation und Deaktivierung brauchen einen eigenen sicheren Admin-Workflow
- OFFEN: Rollen-/Permission-Modell ist minimal und noch nicht fein granular
- OFFEN: UI fuer User, Rollen und Copy-Relationship-Verwaltung fehlt noch
- OFFEN: Audit-Logging fuer fehlgeschlagene autorisierte Aktionen ist noch nicht entschieden

## Risiken
- SHA-256-Token-Hashing ist nur fuer stark zufaellige Tokens geeignet; Tokens muessen serverseitig mit hoher Entropie erzeugt werden
- Ohne Credential-Rotation bleibt der DB-Auth-Pfad administrativ noch manuell
- Der lokale Env-Fallback darf nicht in produktiven Environments verwendet werden

## Verifikation
- `.venv\Scripts\python.exe -m ruff check apps\api infra\migrations` erfolgreich
- `.venv\Scripts\python.exe -m pytest apps\api\tests -p no:cacheprovider` erfolgreich: 27 passed
- `.venv\Scripts\python.exe -m pytest -p no:cacheprovider` erfolgreich: 58 passed
- `.venv\Scripts\python.exe -m alembic upgrade head` erfolgreich
- `.venv\Scripts\python.exe -m alembic current` meldet `20260426_0006 (head)`
- Docker-Stack erfolgreich neu gebaut und gestartet
- `/ready` meldet `postgres=ok`, `redis=ok`, `nats=ok`
- DB-Admin-Smoke erfolgreich: Create/Patch/Audit mit gehashtem DB-Token, Audit-Actor `user`, Smoke-Datensaetze entfernt
