# Shared Events

Schema package boundary for NATS JetStream subjects and event versioning.

The first concrete schemas currently live in `packages/domain`; this package becomes useful once API and workers need generated or separately versioned contracts.